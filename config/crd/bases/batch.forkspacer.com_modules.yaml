---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: modules.batch.forkspacer.com
spec:
  group: batch.forkspacer.com
  names:
    kind: Module
    listKind: ModuleList
    plural: modules
    shortNames:
    - mo
    singular: module
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.source
      name: Source
      type: string
    - jsonPath: .spec.workspace.namespace
      name: Workspace NS
      type: string
    - jsonPath: .spec.workspace.name
      name: Workspace Name
      type: string
    - format: date-time
      jsonPath: .status.lastActivity
      name: Last Activity
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Module is the Schema for the modules API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Module
            properties:
              config:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              createNamespace:
                default: false
                description: |-
                  CreateNamespace specifies whether to create the target namespace if it doesn't exist.
                  Similar to ArgoCD's syncOptions.CreateNamespace.
                type: boolean
              hibernated:
                default: false
                type: boolean
              source:
                properties:
                  configMap:
                    properties:
                      key:
                        minLength: 1
                        type: string
                      name:
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        default: default
                        maxLength: 63
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  existingHelmRelease:
                    properties:
                      chartSource:
                        description: |-
                          ChartSource defines the Helm chart source for managing this release.
                          Required for installation and future operations (upgrades, reconfigurations).
                        properties:
                          configMap:
                            properties:
                              key:
                                minLength: 1
                                type: string
                              name:
                                maxLength: 253
                                minLength: 1
                                type: string
                              namespace:
                                default: default
                                maxLength: 63
                                minLength: 1
                                type: string
                            required:
                            - name
                            - namespace
                            type: object
                          git:
                            description: Git repository containing Helm chart
                            properties:
                              auth:
                                description: Authentication credentials for private
                                  repositories
                                properties:
                                  httpsSecretRef:
                                    description: |-
                                      Reference to a Secret containing Git credentials
                                      username and token fields
                                    properties:
                                      name:
                                        maxLength: 253
                                        minLength: 1
                                        type: string
                                      namespace:
                                        default: default
                                        maxLength: 63
                                        minLength: 1
                                        type: string
                                    required:
                                    - name
                                    - namespace
                                    type: object
                                type: object
                              path:
                                description: Path to the chart directory containing
                                  Chart.yaml
                                minLength: 1
                                type: string
                              repo:
                                description: Repository URL (https or ssh)
                                minLength: 1
                                type: string
                              revision:
                                default: main
                                description: Git revision (branch, tag)
                                type: string
                            required:
                            - path
                            - repo
                            - revision
                            type: object
                          repository:
                            description: Repository-based chart (e.g., from Helm repository)
                            properties:
                              auth:
                                description: Authentication credentials for private
                                  chart repositories
                                properties:
                                  secretRef:
                                    description: |-
                                      Reference to a Secret containing chart repository credentials
                                      username and password fields for basic auth
                                    properties:
                                      name:
                                        maxLength: 253
                                        minLength: 1
                                        type: string
                                      namespace:
                                        default: default
                                        maxLength: 63
                                        minLength: 1
                                        type: string
                                    required:
                                    - name
                                    - namespace
                                    type: object
                                required:
                                - secretRef
                                type: object
                              chart:
                                minLength: 1
                                type: string
                              url:
                                minLength: 1
                                type: string
                              version:
                                type: string
                            required:
                            - chart
                            - url
                            type: object
                        type: object
                      name:
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        default: default
                        maxLength: 63
                        minLength: 1
                        type: string
                      values:
                        description: |-
                          Values allows overriding specific values from the existing release.
                          These values will be merged with the existing release's values.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    required:
                    - chartSource
                    - name
                    - namespace
                    type: object
                  github:
                    type: object
                  httpURL:
                    type: string
                  raw:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              workspace:
                properties:
                  name:
                    maxLength: 253
                    minLength: 1
                    type: string
                  namespace:
                    default: default
                    maxLength: 63
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - hibernated
            - source
            - workspace
            type: object
          status:
            description: status defines the observed state of Module
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Module resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastActivity:
                format: date-time
                type: string
              message:
                type: string
              phase:
                enum:
                - ready
                - installing
                - uninstalling
                - sleeping
                - sleeped
                - resuming
                - failed
                type: string
              source:
                description: Source indicates how this module is managed (e.g., "Adopted/Helm",
                  "Managed/Git")
                type: string
            required:
            - phase
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
