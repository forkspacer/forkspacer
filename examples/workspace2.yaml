apiVersion: batch.forkspacer.com/v1
kind: Workspace
metadata:
  name: default2
  namespace: default

spec:
  # type: kubernetes
  from:
    migrateData: true
    name: default
    # namespace: default
  # connection:
  #   type: kubeconfig
  #   secretReference:
  #     name: vcluster-kube
  # autoHibernation:
  #   enabled: false
  #   schedule: "0 42 18 * * *"
  #   wakeSchedule: "0 44 18 * * *"
